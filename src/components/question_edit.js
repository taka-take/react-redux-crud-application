import { TextField, MenuItem, Button, Paper } from '@material-ui/core';
import { withStyles } from '@material-ui/core/styles';


const QuestionEdit = ({ classes }) => (
    <Paper>
        {/* ここに中身を移動してくる */}
    </Paper>
)

export default QuestionEdit